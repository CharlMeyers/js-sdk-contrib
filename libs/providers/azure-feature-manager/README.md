# azure-feature-manager Provider

## Installation

```
$ npm install @openfeature/azure-feature-manager-provider
```

## Building

Run `nx package providers-azure-feature-manager` to build the library.

## Running unit tests

Run `nx test providers-azure-feature-manager` to execute the unit tests via [Jest](https://jestjs.io).
